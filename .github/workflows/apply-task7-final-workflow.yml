name: Apply Redux Final Workflow

on:
  push:
    branches:
      - redux-implementation
    paths:
      - tools/apply_task7_final_workflow.py
      - .github/workflows/apply-task7-final-workflow.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Apply Task 7 patch
        run: python3 tools/apply_task7_final_workflow.py
      - name: Run Redux static test
        run: python3 tools/test_redux_static.py
      - name: Run SPCC Gaia regression test
        run: python3 tools/test_spcc_gaia_guard.py
      - name: Commit patched Redux source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add AstroTemps_Redux.js
          if git diff --cached --quiet; then
            echo "No Redux source changes to commit."
          else
            git commit -m "feat: add Redux final workflow"
            git push
          fi
